<form method="post" action="{{ path('app_memory_entry_delete', {'id': memory_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ memory_entry.id) }}">
    <button class="btn">Delete</button>
</form>
